import { AppSettings } from "../../src/Services";

export namespace MockHelpers
{
    export const configRoot: any = {
        page: {
            config: {
                rootUrl: "/",
                version: "1.0.0"
            }
        }
    };

}
