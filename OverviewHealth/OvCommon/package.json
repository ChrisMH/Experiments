{
  "name": "@chrismh/ov-common",
  "version": "1.0.1",
  "description": "Overview Health common types",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Hogan",
  "license": "ISC",
  "dependencies": {
    "typedjson-npm": "^0.1.7"
  },
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.3.2"
  }
}
